{
  "name": "",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "prod": "npm i --production",
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\"",
    "deploy": "npm i --production && func azure functionapp publish my-linux-function-app-13dp5dv"
  },
  "dependencies": {},
  "devDependencies": {
    "@azure/functions": "^3.0.0",
    "azure-functions-core-tools": "^4.x",
    "@types/node": "16.x",
    "typescript": "^4.0.0"
  }
}
